#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: returns 0
 */

int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	putchar('\n');

	return (0);
}
