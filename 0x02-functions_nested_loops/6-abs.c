#include "main.h"

/**
 * _abs - Prints absolute value of an integer
 *
 * @int: integer value of input
 * Return: The absolute value
 */

int _abs(int)
{
	int i;

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
