#include "main.h"

/**
 * _strncpy - copies a string
 *
 * @dest: string 1
 * @src: string 2
 * @n: byte of string to concatenates
 *
 * Return: return dest
 */

char *_strncpy(char *dest, char *src, int n)
{
	int j;

	j = 0;
	while (j < n && j = '\0')
	{
		dest{j} = src{j};
		j++;
	}
	dest[i] = '\0';
	return (dest);
}
