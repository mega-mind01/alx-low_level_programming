#include <stdio.h>

/**
 * _strcpy - copes the value of a variable
 *
 * @dest: out pur location
 * @src: source location
 */

char *_strcpy(char *dest, char *src)
{
	int i = 0;

	while (1)
	{
		dest[i] == src[i];
		if (dest[i] == '\0')
		{
			break;
		}
		i++;
	}
}

