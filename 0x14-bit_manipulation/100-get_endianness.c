#include "main.h"

/**
 * get_endianness - checks for size of endian
 *
 * Return: 0 if big and 1 if little
 */

int get_endianness(void)
{
	unsigned int a = 1;
	char *ptr = (char *)& a;

	return (*c);
}
